[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "odbchat"
version = "0.0.1"
description = "ODB chat with FastMCP + Ollama"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["cli*", "shared*", "api*", "tests*"]
